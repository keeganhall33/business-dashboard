name: GA4 Raw Telemetry Refresh

on:
  workflow_dispatch:
  schedule:
    - cron: "15 10 * * *" # 02:15 PT during PDT
  push:
    branches:
      - main
    paths:
      - "scripts/run-ga4-raw-telemetry.mjs"
      - "config/env/website.op.env"
      - "package.json"
      - ".github/workflows/ga4-telemetry-refresh.yml"

permissions:
  contents: read

concurrency:
  group: ga4-raw-telemetry-production
  cancel-in-progress: false

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - uses: 1Password/install-cli-action@v1
      - name: Ingest GA4 raw telemetry into production Supabase
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
        run: |
          test -n "$OP_SERVICE_ACCOUNT_TOKEN" || { echo "OP service account token missing" >&2; exit 1; }
          cp config/env/website.op.env .env.website
          op run --env-file .env.website -- npm run ga4:ingest
