{
  "generated_at": "2026-07-29T22:25:00-07:00",
  "milestone": "M6_action_preparation_and_approval",
  "version": "bi_approval_model_v1",
  "approval_classes": {
    "informational": {
      "purpose": "Internal-only work, no external side effects.",
      "required_for": ["saving a draft", "internal analysis", "non-public outline"]
    },
    "content": {
      "purpose": "Approves final content/copy/assets before publishing.",
      "required_for": ["final email copy", "social copy", "ad copy", "website copy", "outreach drafts"]
    },
    "audience": {
      "purpose": "Approves recipient/audience definitions.",
      "required_for": ["email recipient list", "ad audience", "collector cohort", "lead cohort"]
    },
    "financial": {
      "purpose": "Approves spend/discount/pricing implications.",
      "required_for": ["ad budget changes", "discounts", "pricing", "paid services"]
    },
    "publication": {
      "purpose": "Approves publishing/sending/launching.",
      "required_for": ["send email", "publish social", "publish website change", "launch ads"]
    },
    "customer_contact": {
      "purpose": "Approves direct customer outreach.",
      "required_for": ["collector outreach", "lead follow-up", "abandoned-cart messaging"]
    },
    "data_system": {
      "purpose": "Approves system/data changes.",
      "required_for": ["database changes", "tracking changes", "integration configuration", "data deletion", "backfills", "scheduler changes"]
    }
  },
  "approval_schema": {
    "required_fields": [
      "approval_id",
      "prepared_action_id",
      "action_version",
      "content_hash",
      "requested_at",
      "requested_by",
      "approver",
      "approval_type",
      "status",
      "expires_at"
    ],
    "optional_fields": [
      "decision_at",
      "decision_reason",
      "approved_scope",
      "approved_budget_cents",
      "approved_schedule",
      "conditions",
      "revoked_at",
      "revocation_reason",
      "execution_deadline",
      "metadata"
    ]
  },
  "approval_states": [
    "draft",
    "pending",
    "approved",
    "conditionally_approved",
    "rejected",
    "revoked",
    "expired",
    "superseded",
    "executed",
    "failed"
  ],
  "state_transitions": [
    ["draft", "pending"],
    ["pending", "approved"],
    ["pending", "conditionally_approved"],
    ["pending", "rejected"],
    ["approved", "executed"],
    ["approved", "revoked"],
    ["conditionally_approved", "approved"],
    ["approved", "expired"],
    ["rejected", "superseded"],
    ["executed", "failed"]
  ],
  "invalid_if": [
    "payload changes",
    "content hash changes",
    "budget changes",
    "audience/recipients change",
    "schedule changes materially",
    "target system changes",
    "target object changes",
    "source data stale beyond allowed threshold",
    "recommendation expires",
    "conflicting action approved"
  ]
}
